spring:
  output:
    ansi:
      enabled: always
  jpa:
    hibernate:
      ddl-auto: create #create , create-drop , update , validate, none
    properties:
      hibernate:
        show_sql: true
        format_sql: true